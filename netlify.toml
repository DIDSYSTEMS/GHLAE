[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NEXT_PRIVATE_LOCAL_WEBPACK = "true"
  # SQLite for build-time Prisma client generation
  DATABASE_URL = "file:./dev.db"

[[plugins]]
  package = "@netlify/plugin-nextjs"
